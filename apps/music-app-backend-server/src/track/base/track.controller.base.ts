/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TrackService } from "../track.service";
import { TrackCreateInput } from "./TrackCreateInput";
import { Track } from "./Track";
import { TrackFindManyArgs } from "./TrackFindManyArgs";
import { TrackWhereUniqueInput } from "./TrackWhereUniqueInput";
import { TrackUpdateInput } from "./TrackUpdateInput";
import { TrackStatusFindManyArgs } from "../../trackStatus/base/TrackStatusFindManyArgs";
import { TrackStatus } from "../../trackStatus/base/TrackStatus";
import { TrackStatusWhereUniqueInput } from "../../trackStatus/base/TrackStatusWhereUniqueInput";

export class TrackControllerBase {
  constructor(protected readonly service: TrackService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Track })
  async createTrack(@common.Body() data: TrackCreateInput): Promise<Track> {
    return await this.service.createTrack({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        artist: true,
        duration: true,
        file: true,
        title: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Track] })
  @ApiNestedQuery(TrackFindManyArgs)
  async tracks(@common.Req() request: Request): Promise<Track[]> {
    const args = plainToClass(TrackFindManyArgs, request.query);
    return this.service.tracks({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        artist: true,
        duration: true,
        file: true,
        title: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Track })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async track(
    @common.Param() params: TrackWhereUniqueInput
  ): Promise<Track | null> {
    const result = await this.service.track({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        artist: true,
        duration: true,
        file: true,
        title: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Track })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateTrack(
    @common.Param() params: TrackWhereUniqueInput,
    @common.Body() data: TrackUpdateInput
  ): Promise<Track | null> {
    try {
      return await this.service.updateTrack({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          artist: true,
          duration: true,
          file: true,
          title: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Track })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteTrack(
    @common.Param() params: TrackWhereUniqueInput
  ): Promise<Track | null> {
    try {
      return await this.service.deleteTrack({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          artist: true,
          duration: true,
          file: true,
          title: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/trackStatuses")
  @ApiNestedQuery(TrackStatusFindManyArgs)
  async findTrackStatuses(
    @common.Req() request: Request,
    @common.Param() params: TrackWhereUniqueInput
  ): Promise<TrackStatus[]> {
    const query = plainToClass(TrackStatusFindManyArgs, request.query);
    const results = await this.service.findTrackStatuses(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        status: true,

        track: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/trackStatuses")
  async connectTrackStatuses(
    @common.Param() params: TrackWhereUniqueInput,
    @common.Body() body: TrackStatusWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trackStatuses: {
        connect: body,
      },
    };
    await this.service.updateTrack({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/trackStatuses")
  async updateTrackStatuses(
    @common.Param() params: TrackWhereUniqueInput,
    @common.Body() body: TrackStatusWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trackStatuses: {
        set: body,
      },
    };
    await this.service.updateTrack({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/trackStatuses")
  async disconnectTrackStatuses(
    @common.Param() params: TrackWhereUniqueInput,
    @common.Body() body: TrackStatusWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trackStatuses: {
        disconnect: body,
      },
    };
    await this.service.updateTrack({
      where: params,
      data,
      select: { id: true },
    });
  }
}
